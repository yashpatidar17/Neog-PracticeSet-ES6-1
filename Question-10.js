//Write a function which takes in a number as input and returns it after multiplying by 10.

const multiplyByTen = (a) => (a*10);

console.log(multiplyByTen(20)) // 200
console.log(multiplyByTen(400)) // 400